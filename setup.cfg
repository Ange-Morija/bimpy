[metadata]
name = bimpy
version = 0.0.0
description = BIMpy IFC utilities

[options]
packages = find:
install_requires =
    ifcopenshell
    tqdm

[options.entry_points]
console_scripts =
    bimpy = bimpy.cli:main
